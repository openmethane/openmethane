The daily workflow no longer fails when TROPOMI has no data for the day (e.g. an instrument outage). A metadata-only observation file is produced instead, so WRF, prior and simulated observation outputs are still generated.
