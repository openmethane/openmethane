Added a performance and hardware reference (`docs/reference/performance.md`), covering how to
choose `NUM_PROC_TOTAL` for a machine, why `nproc` is the wrong starting point,
the effect of SMT, how memory and disk scale with the number of ranks, and how
to measure all of it on your own hardware.
