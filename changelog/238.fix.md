Fill the column above CMAQ's top from the retrieval prior rather than from CMAQ's topmost layer, so the simulated column no longer depends on the one layer CMAQ cannot hold. Observation files must be regenerated; `OBS_OPERATOR_VERSION` is bumped to 3 so that stale files are flagged on load.
